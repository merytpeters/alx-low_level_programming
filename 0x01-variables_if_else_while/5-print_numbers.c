#include <stdio.h>

/**
 * main - Entry
 * Description: Prints Single digits
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int x;

	for (x = 0; x <= 9; x++)
		printf("%d", x);

	printf("\n");

	return (0);
}

#include "main.h"

/**
 * _abs - prints absolute value of an integer
 * @num: Any integer
 * Return: 0 (Success)
 */
int _abs(int num)
{
	int;

	if (num < 0)
	{
		_putchar(num);
	return (num * -1);
	}
	else
	{
		_putchar(num);
	return (num * 1);
	}
	_putchar('\n');
}
